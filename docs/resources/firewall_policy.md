---
# generated by https://github.com/hashicorp/terraform-plugin-docs
page_title: "unifi_firewall_policy Resource - unifi"
subcategory: ""
description: |-
  
---

# unifi_firewall_policy (Resource)

Manages Firewall Policies in the UniFi Network Controller.

## Example Usage

```terraform
resource "unifi_firewall_policy" "example" {
  name    = "Block IoT to LAN"
  enabled = true
  action {
    type = "BLOCK"
    # allow_return_traffic defaults to false for BLOCK
  }
  source {
    zone_id = data.unifi_firewall_zone.iot.id
  }
  destination {
    zone_id = data.unifi_firewall_zone.lan.id
  }
  ip_protocol_scope {
    ip_version = "IPV4"
  }
  logging_enabled = true
}
```

<!-- schema generated by tfplugindocs -->
## Schema

### Required

- `enabled` (Boolean)
- `logging_enabled` (Boolean)
- `name` (String)

### Optional

- `action` (Block, Optional) (see [below for nested schema](#nestedblock--action))
- `connection_state_filter` (Set of String)
- `description` (String)
- `destination` (Block, Optional) (see [below for nested schema](#nestedblock--destination))
- `ipsec_filter` (String)
- `ip_protocol_scope` (Block, Optional) (see [below for nested schema](#nestedblock--ip_protocol_scope))
- `schedule` (Block, Optional) (see [below for nested schema](#nestedblock--schedule))
- `source` (Block, Optional) (see [below for nested schema](#nestedblock--source))

### Read-Only

- `id` (String) The ID of this resource.

<a id="nestedblock--action"></a>
### Nested Schema for `action`

Required:

- `type` (String) Valid values: `ALLOW`, `BLOCK`, `REJECT`.

Optional:

- `allow_return_traffic` (Boolean) Defaults to `true` for `ALLOW` and `false` for `BLOCK` or `REJECT`.

<a id="nestedblock--destination"></a>
### Nested Schema for `destination`

Required:

- `zone_id` (String)

Optional:

- `traffic_filter` (Block, Optional) (see [below for nested schema](#nestedblock--destination--traffic_filter))

<a id="nestedblock--destination--traffic_filter"></a>
### Nested Schema for `destination.traffic_filter`

Required:

- `type` (String)

Optional:

- `domain_filter` (Block, Optional)
- `ip_address_filter` (Block, Optional)
- `mac_address` (String)
- `mac_address_filter` (Block, Optional)
- `network_filter` (Block, Optional)
- `port_filter` (Block, Optional)

<a id="nestedblock--ip_protocol_scope"></a>
### Nested Schema for `ip_protocol_scope`

Required:

- `ip_version` (String) Valid values: `IPV4`, `IPV6`, `IPV4_AND_IPV6`.

Optional:

- `protocol_filter` (Block, Optional)

<a id="nestedblock--schedule"></a>
### Nested Schema for `schedule`

Required:

- `mode` (String) Valid values: `EVERY_DAY`, `EVERY_WEEK`, `ONE_TIME_ONLY`, `CUSTOM`.

Optional:

- `days_of_week` (Set of String)
- `start` (String)
- `stop` (String)
- `time_range` (Block, Optional)

<a id="nestedblock--source"></a>
### Nested Schema for `source`

Required:

- `zone_id` (String)

Optional:

- `traffic_filter` (Block, Optional) Same as `destination.traffic_filter`.
